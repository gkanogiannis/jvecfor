% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/JvecforParam.R
\docType{class}
\name{JvecforParam-class}
\alias{JvecforParam-class}
\alias{JvecforParam}
\alias{show,JvecforParam-method}
\alias{buildIndex,matrix,JvecforParam-method}
\alias{findKNN,JvecforIndex-method}
\title{JvecforParam: BiocNeighbors Parameter Class for jvecfor}
\usage{
JvecforParam(
  type = "ann",
  distance = "Euclidean",
  M = 16L,
  ef.search = 0L,
  oversample.factor = 1,
  pq.subspaces = 0L,
  verbose = FALSE
)

\S4method{show}{JvecforParam}(object)

\S4method{buildIndex}{matrix,JvecforParam}(X, transposed = FALSE, ..., BNPARAM = NULL)

\S4method{findKNN}{JvecforIndex}(
  X,
  k,
  get.index = TRUE,
  get.distance = TRUE,
  num.threads = 1,
  subset = NULL,
  ...,
  BNPARAM = NULL
)
}
\arguments{
\item{type}{Character. \code{"ann"} (default) or \code{"knn"}.}

\item{distance}{Character. \code{"Euclidean"} (default) or
\code{"Cosine"}.}

\item{M}{Integer. HNSW max connections per node. Default 16L.}

\item{ef.search}{Integer. HNSW beam width (0 = auto). Default 0L.}

\item{oversample.factor}{Numeric. Oversampling multiplier. Default 1.0.}

\item{pq.subspaces}{Integer. PQ subspaces (0 = disabled). Default 0L.}

\item{verbose}{Logical. Java progress logging. Default FALSE.}

\item{object}{A \code{JvecforParam} object.}

\item{X}{A numeric matrix (rows = observations, cols = features).}

\item{transposed}{Logical. If TRUE, \code{X} is features-by-obs
and will be transposed. Default FALSE.}

\item{...}{Ignored.}

\item{BNPARAM}{Ignored for this method.}

\item{k}{Integer. Number of nearest neighbors.}

\item{get.index}{Logical. Return index matrix? Default TRUE.}

\item{get.distance}{Logical. Return distance matrix? Default TRUE.}

\item{num.threads}{Integer. Thread count. Default 1.}

\item{subset}{Integer vector. Row indices to return results for.
All rows are computed; this filters the output. Default NULL
(all rows).}
}
\value{
A \code{JvecforParam} object.

A \code{\linkS4class{JvecforIndex}} object.

A named list with \code{index} (n-by-k integer matrix or
  NULL) and \code{distance} (n-by-k numeric matrix or NULL).
}
\description{
A \link[BiocNeighbors:BiocNeighborParam-class]{BiocNeighborParam}
subclass for the jvecfor Java backend.
Passing a \code{JvecforParam} object as the \code{BNPARAM} argument to
\code{\link[BiocNeighbors]{findKNN}} or higher-level functions
(e.g. \code{scran::buildSNNGraph}, \code{scater::runUMAP}) routes
neighbor search through jvecfor's HNSW-DiskANN or VP-tree engine.
}
\section{Methods (by generic)}{
\itemize{
\item \code{show(JvecforParam)}: Print a summary of the parameter object.

\item \code{buildIndex(X = matrix, BNPARAM = JvecforParam)}: Build a JvecforIndex from a data matrix.

\item \code{findKNN(JvecforIndex)}: Find k-nearest neighbors using a
JvecforIndex.

}}
\section{Functions}{
\itemize{
\item \code{JvecforParam()}: Constructor for JvecforParam objects.

}}
\section{Slots}{

\describe{
\item{\code{type}}{Character. \code{"ann"} (HNSW-DiskANN, default) or
\code{"knn"} (VP-tree exact).}

\item{\code{M}}{Integer. HNSW max connections per node. Default 16L.}

\item{\code{ef.search}}{Integer. HNSW beam width (0 = auto). Default 0L.}

\item{\code{oversample.factor}}{Numeric. Oversampling multiplier (>= 1).
Default 1.0.}

\item{\code{pq.subspaces}}{Integer. Product-quantization subspaces
(0 = disabled). Default 0L.}

\item{\code{verbose}}{Logical. Enable Java progress logging. Default FALSE.}
}}

\section{Supported distance metrics}{

\code{"Euclidean"} and \code{"Cosine"} (title-case, following
BiocNeighbors convention). The jvecfor-specific \code{"dot_product"}
metric is only available via \code{\link{fastFindKNN}} directly.
}

\section{Limitations}{

\itemize{
  \item \code{queryKNN} is not supported. The Java backend performs
    self-KNN only (all points query against all points in a single
    JVM invocation).
  \item The index built by \code{buildIndex} stores the data matrix
    in R memory; the actual Java HNSW/VP-tree index is rebuilt each
    time \code{findKNN} is called.
}
}

\examples{
library(BiocNeighbors)
p <- JvecforParam()
p

# Custom parameters
p2 <- JvecforParam(type = "knn", distance = "Cosine", M = 32L)

# Use with BiocNeighbors (requires Java >= 20):
# res <- findKNN(X, k = 10, BNPARAM = JvecforParam())

}
\seealso{
\code{\link{fastFindKNN}} for the standalone function with
  full parameter control including \code{dot_product} metric.
}
